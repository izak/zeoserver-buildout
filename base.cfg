[buildout]
extensions =
    mr.developer
    buildout.dumppickedversions

extends =
    versions.cfg

eggs-directory = eggs
 
parts =
    zeoserver
 
[zeoserver]
recipe = plone.recipe.zeoserver
zeo-address = localhost:8100
